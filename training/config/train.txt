# --- Set epochs and save frequency to 1 ---
N_eps = 1
save_model_freq = 1

# --- (Optional but Recommended for Testing) Use a small subset of data to speed it up ---
short = 10 # Use only 10 trajectory folders from the dataset

# Device and paths
device = cpu # Use 'cpu' if you have a GPU, otherwise 'cpu'
basedir = /home/coppholl/Projects/Drone-OA-IREE-ViT-Accelerator 
logdir = training/logs
datadir = training

# Dataset settings
dataset = small_data
short = 0
val_split = 0.2

# Model and checkpoint
# model_type is ignored by qat_ita_trainer.py, which always uses ITALSTMNetVIT
model_type = ITAViTLSTMFloat 

# Training hyperparameters
lr = 1e-4 # Use a smaller learning rate for fine-tuning
N_eps = 5 # Fine-tuning usually requires fewer epochs
lr_warmup_epochs = 5
lr_decay = True
save_model_freq = 10
val_freq = 5

# Workspace suffix for QAT runs
ws_suffix = _float_trainig
